export interface Deal {
    dealRating: string,
    normalPrice: string,
    salePrice: string,
    thumb: string,
    title: string
}

export interface Item {
    title: string,
    salePrice: string
}